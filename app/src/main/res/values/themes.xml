<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose drives the real theme; this is the launch/window theme.
         Dark background avoids a white flash on cold start (good for a wall display). -->
    <style name="Theme.TvPort" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
